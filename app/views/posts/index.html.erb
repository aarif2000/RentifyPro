<h1>All Posts</h1>
<%= link_to 'New Post', new_post_path %>
<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.name, post_path(post) %>
      (<%= pluralize(post.comments.count, 'comment') %>)
    </li>
  <% end %>
</ul>
